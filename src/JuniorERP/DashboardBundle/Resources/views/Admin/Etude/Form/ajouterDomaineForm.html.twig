
<form method="post" {{ form_enctype(form) }} action="{{ path('JuniorERPDashboardBundle_admin_etude_domaine_ajouter') }}" class="leftLabel">
						
	<ul>
		<li>
			{{ form_label(form.nom,null, { 'attr': {'class': 'fldTitle'}}) }}
			<!-- Affichage des erreurs pour ce champ prŽcis. -->
    		{{ form_errors(form.nom) }}

    		<!-- GŽnŽration de l'input. -->
    							
			<div class ="fieldwrap">
				{{ form_widget(form.nom, { 'attr': {'class': 'large texttips'}} ) }}
			</div>
		</li>
		<li>
			{{ form_label(form.reference,null, { 'attr': {'class': 'fldTitle'}}) }}
			<!-- Affichage des erreurs pour ce champ prŽcis. -->
    		{{ form_errors(form.reference) }}

    		<!-- GŽnŽration de l'input. -->
    				
			<div class ="fieldwrap">
				{{ form_widget(form.reference, { 'attr': {'class': 'large texttips'}} )}}
			</div>
		</li>
		
		<li class="buttons bottom-round noboder">
			<div class ="fieldwrap">
				<input name="" type="submit" value="Submit" class="submit-button"><input name="" value="Clear Form" type="reset" class="submit-button">
			</div>
		</li>
	</ul>

	<!-- Les erreurs gŽnŽrales du formulaire. -->
	{{ form_errors(form) }}
						
	 {{ form_rest(form) }}
</form>